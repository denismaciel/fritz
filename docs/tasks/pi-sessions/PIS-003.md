# PIS-003 tree/path replay into model context

## Goal

Rebuild current LLM context from the active branch path, not from raw file order.

## Scope

- branch path walk from `leafId`
- message replay
- model/thinking state replay if we support those entries
- compaction entry hook point in replay
- tree view data structure

## Non-goals

- UI tree navigation
- branch summarization generation

## Pi reference files

- [`/tmp/pi-mono/packages/coding-agent/src/core/session-manager.ts`](/tmp/pi-mono/packages/coding-agent/src/core/session-manager.ts)
  - `buildSessionContext()`
  - `getBranch()`
  - `getTree()`
  - `getLeafId()`
  - `getEntry()`
  - label helpers if we keep labels
- [`/tmp/pi-mono/packages/coding-agent/docs/session.md`](/tmp/pi-mono/packages/coding-agent/docs/session.md)
  - message types
  - tree semantics

## Notes

- this is the real Pi trick: one file, many branches, current context from one path

## Validation

- tests for branch replay
- tests for replay after compaction entry appears
- tests for tree structure build

