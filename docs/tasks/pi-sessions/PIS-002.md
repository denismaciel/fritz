# PIS-002 append-only persistence and delayed flush

## Goal

Port Pi's storage semantics for creating a new session, appending entries, and persisting safely.

## Scope

- create new session file
- append entries
- delayed flush semantics if we want Pi's "write only after first assistant msg"
- in-memory mode seam
- rewrite path for corruption recovery if needed

## Non-goals

- session listing
- compaction
- runtime switching

## Pi reference files

- [`/tmp/pi-mono/packages/coding-agent/src/core/session-manager.ts`](/tmp/pi-mono/packages/coding-agent/src/core/session-manager.ts)
  - constructor
  - `setSessionFile()`
  - `newSession()`
  - `_persist()`
  - `_appendEntry()`
  - `appendMessage()`
  - `appendThinkingLevelChange()`
  - `appendModelChange()`
  - `appendSessionInfo()`
  - `isPersisted()`
- [`/tmp/pi-mono/packages/coding-agent/test/rpc.test.ts`](/tmp/pi-mono/packages/coding-agent/test/rpc.test.ts)
  - session save checks
  - session name persistence checks

## Notes

- Pi delays initial flush until first assistant msg
- decide explicitly if we want exact parity or simpler eager write

## Validation

- tests for append-only writes
- tests for in-memory mode
- tests for initial file creation semantics

