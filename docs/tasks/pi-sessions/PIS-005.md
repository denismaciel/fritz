# PIS-005 runtime host for new/resume/fork switching

## Goal

Add the runtime replacement layer Pi uses so `new`, `resume`, and `fork` swap active session cleanly.

## Scope

- host object around replaceable runtime/session
- switch session
- new session
- fork session
- import jsonl optional if cheap

## Non-goals

- full extension event system
- TUI integration

## Pi reference files

- [`/tmp/pi-mono/packages/coding-agent/src/core/agent-session-runtime.ts`](/tmp/pi-mono/packages/coding-agent/src/core/agent-session-runtime.ts)
  - `createAgentSessionRuntime()`
  - `AgentSessionRuntimeHost`
  - `switchSession()`
  - `newSession()`
  - `fork()`
  - `importFromJsonl()`
- [`/tmp/pi-mono/packages/coding-agent/src/core/agent-session.ts`](/tmp/pi-mono/packages/coding-agent/src/core/agent-session.ts)
  - `setSessionName()`
  - runtime/session access patterns around `sessionManager`

## Notes

- keep this thinner than Pi until we actually need hot runtime replacement

## Validation

- tests for state swap on new/resume
- tests that fork switches active session and preserves selected text where applicable

