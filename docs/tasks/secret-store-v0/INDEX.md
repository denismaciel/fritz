# Secret Store V0

Goal: add a harness-owned secret store so the agent can persist named secrets without putting plaintext into memory/workspace docs or startup env every time.

Tickets:

- `SSV-001` secret store paths + file format
- `SSV-002` store API + tests
- `SSV-003` secret tools
- `SSV-004` runtime wiring + policy
- `SSV-005` docs + operator flow
