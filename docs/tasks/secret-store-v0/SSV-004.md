# SSV-004

## Goal

Wire store into runtime with a minimal policy.

## Scope

- available in local CLI + Telegram runtime
- DM/authorized-user safe path for Telegram
- no secret values in system prompt
- prompt gets only a small rule that secrets use dedicated tools

## Done when

- runtime uses shared store instance
- prompt/tool loop can set/list/delete secrets
- tests prove secrets do not enter prompt context
