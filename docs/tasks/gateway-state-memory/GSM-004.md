# GSM-004: gateway state metadata + reserved bindings layout

**Type:** Task

## Goal

Formalize the remaining gateway state layout so future heartbeat/bindings work has a stable home.

## Places To Change

- `internal/gateway/`
- `plan.md`
- `README.md`

## Scope

- add `meta.json` with version + minimal gateway metadata
- add empty/typed reserved binding store path at `bindings/current.json`
- document the on-disk gateway state layout
- add tests for path creation and metadata round-trip

## Non-goals

- actual binding behavior
- heartbeat queue
- reminder state

## Design Notes

- follow OpenClaw’s “many small stores” shape:
  - `/home/denis/tmp/openclaw-EUwHTl/src/infra/outbound/current-conversation-bindings.ts`
- keep metadata minimal:
  - schema version
  - created/updated timestamps if useful
- do not invent binding semantics yet; reserve the file and type only

## Acceptance Criteria

- gateway state layout is documented and stable
- meta store exists and is tested
- bindings path is reserved for later tickets without behavior leakage

## Dependencies

- `GSM-001`

## Validation

- `go test ./internal/gateway`
- `rg -n "gateway/.+json|bindings/current.json|meta.json" README.md plan.md internal/gateway`
