# EGT-006: docs, regression tests, and CLI switch-over

**Type:** Task

## Goal

Close the pack with docs and regressions, and make the new embedded UI the documented default local interface.

## Places To Change

- `README.md`
- `docs/tasks/embedded-go-terminal-ui/STATUS.md`
- `internal/app/`
- tests across `internal/app/` and `internal/terminalui/`

## Scope

- document local embedded UI behavior and key controls
- add regression tests for local vs remote chat mode selection
- document that `serve` is optional, not required for terminal use
- update pack status to done

## Non-goals

- new browser client
- auth/deployment work
- session/tree UI extensions

## Design Notes

- docs should reflect implemented controls only
- preserve the architectural split:
  - embedded local UI
  - optional server/integration mode

## Acceptance Criteria

- docs explain how to use local embedded UI and remote mode
- tests pin command routing and basic interactive behavior seams
- pack can be closed with validation green

## Dependencies

- `EGT-005`

## Validation

- `go test ./...`
- `uv run scripts/validate_pi_tool_test_parity.py`
- `go run ./cmd/fritz help`
